interface:
  display_name: "Personal Principles"
  short_description: "Capture weighted personal operating principles"
  default_prompt: "Use $track-personal-principles to consult my personal operating principles, surface reusable candidates without persisting them implicitly, and write only when I explicitly request or approve the archive change."
policy:
  allow_implicit_invocation: true
