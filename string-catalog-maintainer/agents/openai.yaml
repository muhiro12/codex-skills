interface:
  display_name: "String Catalog Maintainer"
  short_description: "Audit and repair Xcode string catalogs"
  default_prompt: "Use $string-catalog-maintainer to repair my app's string catalogs, fill missing locale entries, remove deletable stale keys, and report stale keys that still need code-side fixes."
