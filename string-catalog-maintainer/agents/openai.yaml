interface:
  display_name: "String Catalog Maintainer"
  short_description: "Audit and repair Xcode string catalogs"
  default_prompt: "Use $string-catalog-maintainer to audit my app's string catalogs, consult stored localization principles before translating proper nouns or platform feature names, apply reviewed translation patches, prune only verified stale-unused keys, and report stale keys needing code-side fixes."
