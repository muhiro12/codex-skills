interface:
  display_name: "Xcode Preview Auditor"
  short_description: "Audit SwiftUI #Preview with native rendering"
  default_prompt: "Use $xcode-preview-auditor to capture the requested SwiftUI previews and report visible findings and explicit coverage gaps."
