interface:
  display_name: "Xcode Preview Auditor"
  short_description: "Audit SwiftUI #Preview with native rendering"
  default_prompt: "Use $xcode-preview-auditor to audit SwiftUI #Preview screens with the active Xcode-native Preview capability, preserve a per-preview coverage ledger, use live-app fallback only for exactly reproducible states, restore any changed scheme and destination, and keep the run audit-only unless fixes are explicitly requested."
