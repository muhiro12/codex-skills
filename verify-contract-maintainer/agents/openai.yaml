interface:
  display_name: "Verify Contract Maintainer"
  short_description: "Maintain existing verification contracts safely"
  default_prompt: "Use $verify-contract-maintainer to audit, minimally bootstrap, or normalize verification contracts only in repositories that already have or have explicitly decided to adopt ci_scripts, XcodeBuildMCP-first checks, or retained repository-rule scripts; use $apple-repo-verify-bootstrapper instead for first-pass Apple build/test scaffolding."
