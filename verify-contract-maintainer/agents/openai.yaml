interface:
  display_name: "Verify Contract Maintainer"
  short_description: "Maintain existing verification contracts safely"
  default_prompt: "Use $verify-contract-maintainer to audit or normalize an existing native Xcode MCP-first verification contract, including concrete build/test/run/Preview/device actions, active scheme and destination restoration, and retained format/lint/static-rule scripts; use $apple-repo-verify-bootstrapper instead for first-pass Apple verification design."
