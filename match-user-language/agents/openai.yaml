interface:
  display_name: "Match User Language"
  short_description: "Match Codex replies and plans to the user language"
  default_prompt: "Use $match-user-language to keep Codex replies, update_plan text, request_user_input text, automation names/prompts, and other user-facing structured UI text aligned with the user conversation language while leaving code, directives, schema fields, and repository artifacts unchanged."
policy:
  allow_implicit_invocation: true
