interface:
  display_name: "Developer Principles"
  short_description: "Capture weighted cross-repo developer principles"
  default_prompt: "Use $track-developer-principles to consult relevant developer judgment, or update records when explicitly requested."
policy:
  allow_implicit_invocation: true
