interface:
  display_name: "Apple iOS Dev Flow"
  short_description: "Orchestrate local iOS development workflows"
  default_prompt: "Use $apple-ios-dev-flow as the local Apple-platform development orchestrator: start from current repository evidence, consult stored principles when judgment matters, apply HIG/Swift/sample-code gates, route specialized work to OpenAI Build iOS Apps skills and XcodeBuildMCP, use sibling repositories only as read-only fallback evidence, then run the repository's standard verification shell before replying."
policy:
  allow_implicit_invocation: true
