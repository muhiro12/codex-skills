interface:
  display_name: "Repo Momentum Driver"
  short_description: "Pick one safe next repo task per turn"
  default_prompt: "Use $repo-momentum-driver to inspect the current repository, choose exactly one low-risk next task, prefer current-repo evidence first plus stored platform principles when relevant, route Apple-platform implementation through $apple-ios-dev-flow including HIG/Swift/sample-code and Build iOS Apps specialist skills, use sibling repositories only as read-only fallback evidence, implement it, and finish by running the repo's documented verification contract."
