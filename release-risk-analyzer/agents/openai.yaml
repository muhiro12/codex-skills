interface:
  display_name: "Release Risk Analyzer"
  short_description: "Score release-blocking product risks"
  default_prompt: "Use $release-risk-analyzer to score changes since the latest release tag across durable state, irreversible configuration, and release-blocking product risks."
